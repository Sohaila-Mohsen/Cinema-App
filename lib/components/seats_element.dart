import 'package:flutter/material.dart';

class SeatsElement extends StatelessWidget {
  final int? numSeats;
  final List<int>? reservedSeats;
  final String? seatSide;

  SeatsElement({this.numSeats, this.seatSide, this.reservedSeats});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
